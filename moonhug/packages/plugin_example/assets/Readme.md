plugin_example mounted content.

This file lives inside the package (packages/plugin_example/assets/) and
shows up in the editor's Project view under Packages/plugin_example. Its
.meta is committed with the package, so the guid is stable in every project.
