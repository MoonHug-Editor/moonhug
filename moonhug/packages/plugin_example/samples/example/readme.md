Sample folder — inert until installed. Nothing scans samples/: the asset db
only reads the package's assets/ subtree and prebuild only scans the package
root and editor/. To install: copy this folder to moonhug/packages/<name>/
and it becomes an ordinary package.
